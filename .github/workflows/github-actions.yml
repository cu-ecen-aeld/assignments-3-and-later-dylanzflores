name: assignment-test
on:
  push:
    tags-ignore:
      - '*'
    branches:
      - '*'
jobs:
  # Uncomment this job if you want to run unit tests
  # unit-test:
  #   container: cuaesd/aesd-autotest:24-unit-test
  #   runs-on: self-hosted
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         submodules: true
  #     - name: Run unit test
  #       run: ./unit-test.sh

  full-test:
    container: cuaesd/aesd-autotest:24-assignment5
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      - name: Run full test
        run: ./full-test.sh

